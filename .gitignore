node_modules/
.env
*.log
.DS_Store
Thumbs.db

.vercel
.env*

.vercel
.env*

# Database copies. These hold real customer records — names, phone numbers,
# addresses, loan amounts — and must never reach the repository.
backups/
*.sql
*.dump
